# Server-only secrets — never commit real keys.
# Set OPENAI_API_KEY in Railway (or your host) environment variables.

OPENAI_API_KEY=

# Mobile (Expo) — point at your deployed TanStack Start origin (no trailing slash)
# EXPO_PUBLIC_API_URL=https://your-app.up.railway.app
